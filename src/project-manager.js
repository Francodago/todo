const blankProjectLoad = () => {
  let projectArray = [];
  let projectTitle = 'Default Project';
  projectArray.push({ projectTitle });
  return { projectArray, projectTitle };
};
export default blankProjectLoad;
